[tools]
java = "openjdk-25"

[env]
# backend dev port (frontend dev server takes 8080)
PORT = "8090"
# Zitadel (local) — see README for setup. These are not secrets; PKCE public client.
AUTH_ISSUER = "http://localhost:8081"
AUTH_CLIENT_ID = "374810488246173699"
AUTH_AUDIENCE = "374810419610517507"
AUTH_PROJECT_ID = "374810419610517507"
AUTH_REDIRECT_URI = "http://localhost:8080/auth/callback"
AUTH_POST_LOGOUT_REDIRECT_URI = "http://localhost:8080/"
AUTH_JWKS_URL = "http://localhost:8081/oauth/v2/keys"
